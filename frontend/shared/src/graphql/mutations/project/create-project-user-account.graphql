#import "../../../../../shared/src/graphql/fragments/project-user-account-fragment.graphql"

mutation CreateProjectUserAccountMutation($projectId: String!, $userAccountId: String!) {
  createProjectUserAccount(projectId: $projectId, userAccountId: $userAccountId) {
    ...ProjectUserAccountFragment
  }
}
