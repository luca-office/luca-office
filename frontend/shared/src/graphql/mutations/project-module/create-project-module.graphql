#import "../../fragments/project-module-fragment.graphql"

mutation CreateProjectModuleMutation($creation: ProjectModuleCreation!) {
  createProjectModule(creation: $creation) {
    ...ProjectModuleFragment
  }
}
