#import "../../fragments/survey-participation-info-fragment.graphql"

query SurveyParticipationInfoQuery($token: String!) {
  surveyParticipationInfo(token: $token) {
    ...SurveyParticipationInfoFragment
  }
}
