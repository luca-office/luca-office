import * as latestStartedProjectModule from "./latest-started-project-module.graphql"
import * as survey from "./survey.graphql"
import * as surveyInvitation from "./survey-invitation.graphql"
import * as surveyInvitations from "./survey-invitations.graphql"
import * as surveyInvitationsProgress from "./survey-invitations-progress.graphql"
import * as surveyLight from "./survey-light.graphql"
import * as surveyParticipationInfo from "./survey-participation-info.graphql"
import * as surveyUserAccounts from "./survey-user-accounts.graphql"
import * as surveys from "./surveys.graphql"
import * as surveysForUserAccount from "./surveys-for-user-account.graphql"

export const latestStartedProjectModuleQuery = latestStartedProjectModule
export const surveyInvitationQuery = surveyInvitation
export const surveyInvitationsProgressQuery = surveyInvitationsProgress
export const surveyInvitationsQuery = surveyInvitations
export const surveyParticipationInfoQuery = surveyParticipationInfo
export const surveyQuery = survey
export const surveysForUserAccountQuery = surveysForUserAccount
export const surveysQuery = surveys
export const surveyUserAccountsQuery = surveyUserAccounts
export const surveyLightQuery = surveyLight
