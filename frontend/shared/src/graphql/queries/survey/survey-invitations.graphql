#import "../../fragments/survey-invitation-light-fragment.graphql"

query SurveyInvitationsQuery($surveyId: String!) {
    surveyInvitations(surveyId: $surveyId) {
        ...SurveyInvitationLightFragment
    }
}
