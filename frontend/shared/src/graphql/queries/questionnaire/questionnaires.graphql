#import "../../fragments/questionnaire-fragment.graphql"

query QuestionnairesQuery($isRuntimeSurvey: Boolean!) {
  questionnaires(isRuntimeSurvey: $isRuntimeSurvey) {
    ...QuestionnaireFragment
  }
}
