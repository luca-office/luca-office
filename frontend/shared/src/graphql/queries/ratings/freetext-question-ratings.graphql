#import "../../fragments/freetext-question-rating-fragment.graphql"

query FreetextQuestionRatingsQuery($ratingId: String!) {
  freetextQuestionRatings(ratingId: $ratingId) {
    ...FreetextQuestionRatingFragment
  }
}
