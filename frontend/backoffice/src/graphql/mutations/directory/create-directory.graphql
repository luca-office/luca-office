#import "../../fragments/directory-fragment.graphql"

mutation CreateDirectoryMutation($creation: DirectoryCreation!) {
    createDirectory(creation: $creation) {
        ...DirectoryFragment
    }
} 