#import "../fragments/survey-invitation-fragment.graphql"

mutation StartOpenParticipationMutation($surveyId: String!) {
  startOpenSurveyParticipation(surveyId: $surveyId) {
    ...SurveyInvitationFragment
  }
}
